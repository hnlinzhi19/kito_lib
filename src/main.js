import { chargeAction, goUserPage } from "./scheme";
import {
  initClient,
  handleUploadImage,
  handleClipboard,
  handleShare,
  handleDownloadImage,
  handleButtons,
  playHorseView,
  enableRefresh
} from "./client";

export default {
  chargeAction,
  goUserPage,
  initClient,
  handleUploadImage,
  handleClipboard,
  handleShare,
  handleDownloadImage,
  handleButtons,
  playHorseView,
  enableRefresh
};
